{
  "version": "0.1.0",
  "scripts": {
    "clean": "rimraf dist release",
    "build": "node scripts/build.mjs",
    "package": "node scripts/package-extension.mjs",
    "test": "node --test tests\\*.test.js",
    "typecheck": "tsc --noEmit",
    "validate": "npm run typecheck && npm test && npm run build && node scripts/validate-dist.mjs && npm run package"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "rimraf": "^6.1.3",
    "typescript": "^7.0.2",
    "yazl": "^3.3.1"
  }
}
