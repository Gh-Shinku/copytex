{
  "scripts": {
    "test": "node --test tests\\*.test.js",
    "typecheck": "tsc --noEmit",
    "validate": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
